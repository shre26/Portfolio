name: Push Success Message

on: [push]

jobs:
  success-message:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Print success message
        run: echo "✅ Push successful! Keep building your portfolio!"
